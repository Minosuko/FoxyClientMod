package com.foxyclient.module.world;
import com.foxyclient.module.Category; import com.foxyclient.module.Module;
/** SafeMine - Prevents mining blocks that would cause damage. */
public class SafeMine extends Module { public SafeMine() { super("SafeMine", "Prevent dangerous mining", Category.WORLD); } }
